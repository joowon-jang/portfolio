# JOOWON.EXE 디자인 시스템 (Design System)

이 문서는 레트로 픽셀 포트폴리오(JOOWON.EXE)의 디자인 원칙, 색·타이포 토큰, 컴포넌트 규칙을 정의합니다.
UI를 수정·추가할 때(사람이든 AI 에이전트든) 이 명세를 준수하십시오. 모든 토큰의 원본은 `css/style.css`의 `:root`입니다.

## 컨셉

**CRT 화면 위에서 돌아가는 레트로 게임 UI.** 포트폴리오 전체를 게임 세이브 파일처럼 표현합니다 — 경력은 CHAPTER/MISSION/LOG, 프로젝트는 QUEST, 성과는 SAVE FILE. 각지고(round 없음), 그림자는 딱딱하고(blur 없음), 모션은 계단식(steps)입니다.

## 컬러 토큰 (Color Tokens)

팔레트는 픽셀아트 표준 팔레트 **Sweetie 16** 계열입니다. 아래 토큰 외의 색을 새로 만들지 마십시오.

| 토큰 | 값 | 역할 |
| :--- | :--- | :--- |
| `--bg` | `#1a1c2c` | 메인 배경 |
| `--bg-panel` | `#29366f` | 카드·패널 배경, 하드 섀도 색 |
| `--bg-deep` | `#20263e` | 한 단계 가라앉은 배경 (인셋 영역, hover 배경) |
| `--fg` | `#f4f4f4` | 기본 텍스트, 주요 테두리(3–4px 프레임) |
| `--cyan` | `#73eff7` | **인터랙션** — 링크, 버튼, hover 섀도, 커서 |
| `--green` | `#a7f070` | **상태·성과** — 날짜, 활성 표시, VERIFIED 뱃지 |
| `--yellow` | `#ffcd75` | **섹션 제목·강조** — 패널 타이틀, 트러블 마커, 포커스 링 |
| `--muted` | `#94b0c2` | 보조 텍스트 |
| `--dim` | `#566c86` | 비활성 텍스트, 보조 테두리(2px) |
| `--body-text` | `#c8d2de` | 본문 긴 글 |
| `--line-soft` | `#34445f` | 최약 구분선(1px) |
| `--focus` | `#ffcd75` | 포커스 아웃라인 전용 |

### 적용 규칙

1. **레이어 구분은 배경색 대비로**: `--bg` → `--bg-deep` → `--bg-panel`. 블러·유리 효과·그라데이션 남발 금지 (픽셀 씬 장식 배경의 하드 스플릿 그라데이션만 예외).
2. **cyan은 "누를 수 있는 것"에만**: 링크·버튼·hover 반응. 장식용 남발 금지.
3. **green은 "이미 이룬 것"**: 날짜·성과·활성 상태. **yellow는 "지금 봐야 할 것"**: 섹션/패널 제목, 문제 상황 마커.
4. 텍스트 위계: `--fg`(제목·강조) > `--body-text`(본문) > `--muted`(보조) > `--dim`(비활성).

## 타이포그래피 (Typography)

| 토큰 | 값 | 용도 |
| :--- | :--- | :--- |
| `--font-pixel` | `'Galmuri11', monospace` | 제목, 레이블, 버튼, 칩 등 모든 "게임 UI 텍스트" |
| `--font-body` | `'Pretendard Variable', Pretendard, …` | 본문 문단 — 가독성이 필요한 긴 글 |

- 기본: `16px / line-height 1.6`. 픽셀 폰트는 실제 렌더링이 작아 보이므로 **가독성 최소선**을 지킵니다 — 읽어야 하는 텍스트는 `.75rem`(12px) 미만 금지, `.625rem` 이하는 순수 장식(칩·데코)에만 허용.

### 타입 스케일

| 단계 | 크기 | 대상 |
| :--- | :--- | :--- |
| 페이지 타이틀 | `clamp()` 2~4rem | hero 이름, 상세 제목 |
| 섹션 타이틀 | `1.5rem` | 홈 `■` 섹션 제목 — 뒤에 픽셀 대시 라인(`::after`, currentColor 30%)이 남은 폭을 채워 시각적 앵커 역할 |
| 창 타이틀바 | `1rem` | `.window-bar` |
| 카드 타이틀 | `1.125 ~ 1.375rem` | 미션 카드 이름, 경력 프로젝트명, 회사명 |
| 항목 타이틀 | `.9375rem` | feature 이름, 타임라인 제목, 트러블 문제 |
| 서브타이틀 | `.875rem` | `.window-subtitle`, 스택 그룹 제목, 기여 로그 제목 |
| 본문 | `.9375 ~ 1rem` | text-list, 설명문, 타임라인·트러블 본문 |
| 보조 텍스트 | `.875rem` | 카드 요약, 메타 정보 |
| 레이블·캡션 | `.75 ~ .8125rem` | STATUS 라벨, 날짜, 캡션, 버튼, 칩 |

- 텍스트 색 위계: 제목 `--fg`/역할색 > 본문 `--body-text` > 보조 `--muted` > 비활성 `--dim`. **읽어야 하는 본문에 `--dim` 사용 금지.**
- **콘텐츠는 불릿 우선**: 여러 문장을 한 문단으로 잇지 않습니다. 서비스 요약·역할·성과 등 나열 가능한 내용은 "한 항목 = 한 문장(또는 구)"의 불릿 리스트(`.text-list`, `.timeline-points`)로 씁니다. 긴 문단은 트러블 해결 설명처럼 서사가 필요한 곳에만 1~2문장으로 허용합니다.
- 픽셀 폰트 제목은 `font-weight: 400` 고정 — 볼드로 두께를 흉내 내지 않습니다. 크기·색으로 위계를 만듭니다.
- 대형 타이틀은 `clamp()` 사용 (예: hero `clamp(2.5rem, 6vw, 4rem)`).
- 한국어 줄바꿈이 어색한 곳은 `word-break: keep-all`.

## 테두리·그림자 (Borders & Shadows)

- **`border-radius` 금지.** 모든 모서리는 직각입니다.
- 테두리 두께 위계: **4px `--fg`** (최상위 프레임: contact-box, brief-hud) > **3px** (주요 카드) > **2px `--dim`** (보조 카드·칩) > **1px `--line-soft`** (구분선).
- 그림자는 **하드 섀도만**: `box-shadow: Npx Npx 0 <색>` — blur 반경은 항상 `0`. 색은 `--bg-panel`/`--bg-deep`(정적), `--cyan`(hover), `rgba(0,0,0,.2~.4)`(장식).
- 섹션 구분 유틸: `.section-minor`(1px) / `.section-standard`(3px) / `.section-major`(4px).

## 모션 (Motion)

- 레트로 감성 유지: 깜빡임은 `steps`/`step-end` (`blink`), 게이지는 `steps(10, end)` (`stat-load`).
- hover/전환은 **0.12s** 고정. 카드 hover는 `translate(-3~-4px, -3~-4px)` + cyan 하드 섀도 증가 패턴.
- 등장 모션은 `fadeup`(0.35~0.5s) 하나로 통일. 떠다니는 무한 루프 장식 금지.
- **`prefers-reduced-motion: reduce` 대응 필수** — 새 모션을 추가하면 반드시 해당 블록에서 꺼야 합니다.

## 반응형 (Breakpoints)

데스크톱 우선(`max-width`) 4단계:

| 기준 | 대상 |
| :--- | :--- |
| `940px` | 내비게이션 → 햄버거 메뉴 전환 |
| `860px` | 태블릿 — 그리드 축소(3→2열), 여백 축소 |
| `720px` | 모바일 — 대부분 1열, case 레이아웃 세로 전환 |
| `420px` | 소형 모바일 — 타이포 축소, 버튼 풀폭 |

- 최소 지원 폭 `320px` (`body { min-width: 320px }`).
- 콘텐츠 최대 폭: 홈 `1180px`, 상세 `980px`.

## 접근성 (Accessibility)

- 포커스: `:focus-visible`에 `3px solid var(--focus)` + `outline-offset: 4px`. 커스텀 포커스 스타일로 대체하지 않습니다.
- 터치 타겟 최소 `44px` (`min-height: 44px` 패턴 유지).
- skip-link, `aria-current`(현재 내비), `aria-pressed`(언어 토글) 등 기존 ARIA 패턴을 새 컴포넌트에도 적용.
- 정보를 색으로만 전달하지 않기 — 마커 문자(`▶`, `◆`, `⚠`, `→`)를 병행합니다.

## 핵심 컴포넌트 패턴

- **윈도우(`.window`)**: 프로젝트 상세 페이지의 기본 단위. 3px `--fg` 프레임 + 하드 섀도 + 반전 타이틀바(`.window-bar` — `--fg` 배경, `--bg` 글자, 우측에 `▚▚▚` 장식 또는 `.window-meta` 보조 텍스트). 강조 변형은 타이틀바·테두리 색 교체(`.window-outcome` green). 본문은 `.window-body`(24px 내외 패딩).
- **상세 페이지 구조**: 사이드바 없이 전 요소가 본문 전체 폭을 씁니다(스크린샷 크기 확보). 히어로(라벨·제목·요약) → **프로젝트 정보 창(`.project-info`)**: 시기와 기간을 한 셀에 묶고 역할별 팀 구성·내 역할을 나란히 비교하는 3열 표, 링크는 하단 보조 행에 배치 → **미션 브리핑(`.window-brief`)**: 문제·기여·성과 3열 창 → `.detail-main` 윈도우 스택. **성과(검증된 결과)를 최상단에** 두고 개요 → 스크린샷 → 기능 → 타임라인 → 문제 해결 → 기술 순서로 배치합니다. 창 제목은 본문 언어로 씁니다(영문 고정 금지). 모바일(≤720px)에서는 각 표 행을 레이블이 붙은 세로 기록으로 전환합니다.
- **타임라인(`.timeline`)**: 개발 단계 서사용. 3px `--line-soft` 세로선 + cyan 사각 노드, 픽셀 폰트 제목(`.timeline-title`) + 본문 폰트 설명(`.timeline-desc`).
- **패널(`.panel`)**: 2px `--dim` 테두리의 범용 상자. 현재는 안내문(`.panel-todo`, dashed) 용도로만 사용.
- **HUD(`.achievement-hud`)**: 홈 히어로의 성과 상태창. `--fg` 프레임 + 반전 헤더 + 3열 그리드(모바일 1열).
- **칩(`.chip`, `.stack-chip`)**: 2px 현재색 테두리, hover 시 배경/글자색 반전.
- **버튼 링크(`.link-btn`)**: cyan 배경 + 하드 섀도, hover 시 `translate(-2px,-2px)` + 섀도 확대.
- **트러블(`.trouble`)**: yellow 4px 좌측 보더 + `--bg-deep` 배경. 문제(픽셀 폰트) → 해결(본문 폰트) 구조.
- **픽셀 씬(`.pixel-scene`)**: 이미지가 없는 슬롯을 CSS로 그린 픽셀아트로 채움. `image-rendering: pixelated`, 도형은 border/box-shadow/clip-path 조합.

## 금지 사항 (Hard Rules)

1. `border-radius` 사용 금지.
2. blur가 있는 `box-shadow`·`backdrop-filter`·글로우 효과 금지.
3. `:root` 토큰 외 새로운 색상 하드코딩 금지 (필요하면 토큰을 추가하고 이 문서에 기록).
4. 픽셀 폰트에 `font-weight` 볼드 금지.
5. 부드러운 ease 장시간 모션·무한 장식 루프 금지 — steps 기반, 0.12s 전환 유지.
6. 새 인터랙티브 요소에 44px 미만 터치 타겟·포커스 스타일 제거 금지.

## 용어 표기 (Terminology)

- 한국어 사용자 노출 문구는 `프론트엔드`, `리팩터링`, `모션`, `미디어 쿼리`, `저장소`로 통일합니다.
- 기술명은 공식 표기인 `Socket.IO`, `React`, `TypeScript`, `TanStack Query`, `nginx`를 사용합니다.
- 동일한 작업 단계를 가리킬 때는 `최초 개발`, `1차 리팩터링`, `2차 리팩터링`을 일관되게 사용합니다.
